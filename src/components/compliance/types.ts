import { LucideIcon } from "lucide-react";

export interface ComplianceMetric {
  title: string;
  value: string;
  change: string;
  icon: LucideIcon;
  color: string;
  bgColor: string;
}
